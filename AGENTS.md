# AGENTS.md · 给 AI 助手的工作目录

> 这是你进入本仓库后 **第 1 份必须读完后才开始动手** 的文档。
> 本项目已经过 14 轮迭代（P0~P14），有大量隐性的历史约定（源码扫描测试、mixin 组合、版本 CI 机制），
> 不看下面三份文档就改代码，几乎必然踩坑或把已有的东西改坏。

## 0. 动手前必读（按此顺序，缺一不可）

1. **`PROJECT_GUIDE.md`** —— 项目结构与维护指南（5 分钟定位任何问题）。这是你的"维修导航"
   （目录地图、mixin 组合模式、测试契约、版本/CI 机制、按症状快速定位）。改代码前必读 §3、§4。
2. **`docs/工作思路与后续规划.md`** —— P1~P14 逐层工作思路 + 后续内容分级规划 + 离线叙事管线拼接
   顺序与四条铁律。**想加新功能/新玩法优先看这里**，避免重复造轮子或破坏系统互斥。
3. **`.github/archive/框架对照维护台账.md`** —— 各阶段需求/实现状态对照 + "明确不做"清单。
   判断"某需求做没做过 / 该不该做"时查这里。

如果只需要修一个 bug，至少读 `PROJECT_GUIDE.md` 的 §4（测试契约）和 §5（版本 CI）再动代码。

## 1. 最重要的三条铁律（违反必出问题）

- **老档兼容是铁律**：`models/` 里任何新字段，`fromJson` 必须给缺省值（旧存档不能崩）。
- **源码扫描测试**：`test/` 有一批会读 `lib/` 源码文本并断言其中字符串/结构的测试。改了源码不同步
  断言，测试必挂。详见 `PROJECT_GUIDE.md` §4。
- **勿手动改版本/CHANGELOG**：`pubspec.yaml` 的 version 是唯一事实来源；CI 自动 bump 版本并 sync
  CHANGELOG。想写说明就放进 commit message body 或用 `UPDATE_DESC.md`。

## 2. 新增一个"世界在动"内容层的标准流程（八步）

见 `PROJECT_GUIDE.md` §3.1 与 `docs/工作思路与后续规划.md` 第五节。核心：
数据表收口到 `data/`，逻辑收口到对应 `mixin_`，接入 `mixin_narrative.dart` 离线管线并遵守互斥/冷却，
共享测试夹具 `makeGame` 里默认关闭新增随机内容，专项测试独立成 `batchNN` 文件。

## 3. 关于本文件

- 本文件是为了让未来的 AI 助手**一进仓库就知道该读哪、别踩哪些坑**。
- 若项目大改导致上述三份文档路径失效，请同步更新本文件，别留死引用。