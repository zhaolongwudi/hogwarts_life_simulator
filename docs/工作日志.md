# 工作日志 · 供下次 AI 助手接力

> 这是给**下一个进入本仓库的 AI** 的交接日志。请先读它 + `AGENTS.md` + `PROJECT_GUIDE.md` §3/§4 +
> `docs/工作思路与后续规划.md`，再动手。
> 每次重要改动后请更新本文件（新增「日期·主题」小节），让接力保持连续。

---

## 环境与恢复（务必先看 —— 沙箱会重置）

本仓库托管在 **GitHub：`zhaolongwudi/hogwarts_life_simulator`**（公开可读）。
**`/data/user/work/hogwarts` 旧路径已被沙箱重置清空**，工程已迁至 `/workspace/hogwarts_life_simulator`。

恢复步骤（若再遇沙箱重置）：
1. `git clone https://github.com/zhaolongwudi/hogwarts_life_simulator /workspace/hogwarts_life_simulator`
2. Flutter 未装时先装 stable（本次用 3.47.4，解压到 `/opt/flutter`，内部 git 需
   `git config --global --add safe.directory /opt/flutter`）。
3. 在工程目录 `flutter pub get`，然后 `flutter analyze`（门禁 0 error）/ `flutter test`（当前 1984 全绿）。
4. **推送鉴权**：远程需写入令牌。已用个人访问令牌注入
   `git remote set-url origin https://<PAT>@github.com/zhaolongwudi/hogwarts_life_simulator.git`。
   令牌若失效请向用户索取新的。

---

## 项目铁律（改代码前过一遍）

- **老档兼容**：`models/` 新字段 `fromJson` 必须给缺省值。
- **源码扫描测试**：`test/` 有读 `lib/` 源码文本并断言的测试，改源码不同步断言必挂。
- **勿手动改版本/CHANGELOG**：`pubspec.yaml` version 是唯一来源；CI push 时自动 bump + sync CHANGELOG。
- **色板**：语义色一律 `MiuiColors`（`lib/theme/miuix_tokens.dart`）；`AppColors` 已是转发别名，仅过渡用；
  学院色唯一入口 `UiHelpers.getHouseColor`；**别在页面裸写 `Color(0xFF...)`**。
- **UI 重构进度与边界**：见 `docs/UI重构规范与台账.md`。

---

## 本次会话工作（2026-09-18）

### 背景
- 上一会话在旧路径 `/data/user/work/hogwarts` 完成了 UI 大规模重构的大部分（统一组件库、
  空态/加载态归一、色板收敛、P9–P14 入口），门禁始终 `flutter analyze` 0 error + `flutter test` 1984 全绿。
- 每次重构后都同步上传 `docs/UI重构规范与台账.md`。
- 本次因沙箱重置丢失本地工程，已从 GitHub 克隆恢复；用户在 `/workspace` 另存了两份
  `hogwarts-offline-mode-review*.html` 离线审查报告（交付物，非工程源码）。

### 关键重构决策速览
- **关于新手引导**：用户明确「不要做新手引导了」，**停止** Phone tab 玩法入口的 `onInfo` 引导方向
  （上一会话已合并的 FeatureTile.onInfo 引导能力保留、无害，但**不再往该方向加内容**）。

### 本轮完成（提交 `0137b42`）
1. **Task A · 学院色单源化到 MiuiColors**
   - `lib/utils/ui_helpers.dart`：`UiHelpers.getHouseColor` 从「深品牌色
     （gryffindor#740001 / slytherin#1A472A / ravenclaw#0E1A40）」收口到
     `MiuiColors` 学院 token（亮色版、暗底更可读）；默认与教职工统一 `houseNeutral`。
   - **删除死代码** `getHouseColorBright`（定义后从无调用）。
   - `lib/screens/game/game_world_tab.dart`：删除自带第三套金/琥珀 switch `_getHouseColor`
     （#B8860B/#3B82F6…，且只在第一个字符大写时匹配成功 —— 潜在 bug），改为委托
     `UiHelpers.getHouseColor` + 仅保留 `staff` 特例（`MiuiColors.onSurfaceVariantActions`）；
     顺带清掉 4 处页面裸色 `Color(0xFF...)`。
   - 改动后学院色在 5 个页面（通讯录/相亲/好感/私聊/世界）+ 世界页**统一为 Miui 亮色学院 token**。

2. **Task B · 设置页两个选择器去重**
   - `lib/screens/settings/settings_preset_pickers.dart`：`buildModePicker` 与 `buildEraPicker`
     原各持一份几乎相同、后又各自跑偏的卡片脚手架（~70 行 ×2）。抽为私有 `_optionCard`
     单一实现。选中底保留历史 `0xFF740001 @ 20%`（`Color(0x33740001)`）外观不变；
     统一 Era 标题色为 Mode 规则（onSurface→white，肉眼无差）。调用方均未用 `icon/color` 分支，行为不变。

3. **文档同步**：`PROJECT_GUIDE.md` §3.5 更新为「语义色一律 MiuiColors + AppColors 兼容别名」；
   `docs/UI重构规范与台账.md` 新增「四·附·E」。

**门禁**：`flutter analyze` 0 error；`flutter test` 1984 全绿；已推送 `0137b42`（CI 自动 bump 版本并 sync CHANGELOG）。

### 审计结论（Task B 扩面排除项 —— 记住，别重复造轮子）
- `settings_scene_routing.dart` 的「场景-提供商」chip 与 `settings_provider_card.dart` 的
  「模型预设」chip **看似重复实则语义不同**（前者带锁图标/禁用态、后者带配额提示），
  **强行合并属过度抽象，跳过**。
- `game_play_screens.dart` 委托板/装备栏空态、world_map 绿系主题色等，按台账「收编边界」
  **作为页面主题精修保留、不推倒**；`_providerColor` 里 DeepSeek/SenseNova 品牌色按边界不做语义错配、保留。

---

## 推荐的下一个方向（供参考，未动工）

1. **收尾 UI 重构的剩余结构债**：UI 层仍有约 700 处历史裸色 `Color(0xFF...)`（台账在逐步收敛）。
   可继续逐页替换为 `MiuiColors` token，但需**逐页核对语义**、不要一刀切，改动后跑全量测试。
2. **world_map_screen.dart 的绿系主题色**：台账边界「精修不推倒」，若确要动，需先在
   `docs/UI重构规范与台账.md` 更新边界说明，再小步收敛。
3. **新功能/玩法**：一律先读 `docs/工作思路与后续规划.md`（P1–P14 思路 + 后续分级规划+
   离线叙事管线拼接顺序与铁律）+ `PROJECT_GUIDE.md` §3.1 八步流程。
4. **版本/CI**：勿手动改版本；CI 会自动 bump。详细说明写 commit message body 或 `UPDATE_DESC.md`。

### 若某方向标注「工作日志存在但被打断」
说明该方向在上次会话只做到一半，接力时先复读本日志与台账的对应小节，确认当前状态再继续，
避免重复或推倒已完成部分。