# 霍格沃兹 · 第十次审查记录

> 依据：**第九次审查记录（游戏系统策划视角）**
> 取证方式：**源码静态取证 + 沙箱内实跑**
> 范围：第九次审查后全部修复轮改动，覆盖 15 个文件、+877/-23 行
> 结论一句话：**三个 P0 机制缺口（数值钝化、维系衰减、节拍概率化）全部收口，P1 八项中六项落地，两项因 UI/产品决策标记为后续规划；新增信息密度调节器、学年里程碑、传闻传播模型三项机制，整体从"玩 20 回合的惊艳 Demo"向"可闭环的长线剧本"迈出实质性一步。**

---

## 0｜基线

| 项 | 数字 |
|---|---|
| Flutter / Dart | 3.47.2 / 3.13.2 |
| 改动文件 | 15 个（`balance_constants.dart` / `goal_data.dart` / `monthly_event_data.dart` / `world_rules.dart` / `mixin_narrative.dart` / `mixin_narrative_continuity.dart` / `mixin_play.dart` / `mixin_relations.dart` / `mixin_response_affection.dart` / `mixin_systems.dart` / `npc.dart` / `choice_prompts.dart` / `narrative_prompts.dart` / `summary_prompts.dart` / `game_provider_base.dart`） |
| 净增代码 | +877 / -23 行 |

---

## 1｜P0 收口

### 1.1 P0-1 好感压缩一刀切 → 分段映射（`mixin_response_affection.dart`）

原公式：
```dart
if (delta > 5) delta = (delta * 0.5).round().clamp(1, 5);
```
救命之恩 +20 和顺手帮忙 +8 落地都是 +5，大事件与日常的数值反馈完全无差别。

改后：
- `Balance.compressAffectionDelta` 分段映射：0-5 原样、6-10→5-7、11-20→7-9、20+→±10
- `rawDelta → severity` 结仇通道不变，赎罪门（actualChange ≥ 8）从此能开
- `_applySocialCost` 新增连续互动递减系数（连续 3 回合后每回合 -20%，地板 30%）

### 1.2 P0-2 好感维系衰减（`mixin_response_affection.dart`）

原系统无任何回落机制，玩家可把一排 NPC 刷到 85+ 后放着不管。

新增：
- `lastAffectionTouchDay` 字段入档（老存档 -1 豁免）
- 30 天无互动每周淡 1~2 点，地板 10
- 快进模式按周补结（单次 ≤ 4 周）
- 重大事件免疫：救命/牺牲/告白/背叛等关键词匹配时跳过衰减，保护关键叙事节点

### 1.3 P0-3 导演节拍概率化（`director_beat_data.dart` 联动）

原节拍 `turn % 3` 固定相位，可预测 + 场景错配。

改后：
- `DirectorBeat` 种类从 3 种扩展为 5 种（`advance` / `daily` / `turn` / `calm` / `social`）
- 概率权重化：`advance` 30% / `daily` 25% / `turn` 25% / `calm` 10% / `social` 10%
- 自定义指示器场景下权重翻倍，增强 `advance` 沉浸感
- `turnsSinceLastTurnBeat` 追踪最近一次转折回合，用于安静期检测

---

## 2｜P1 收口

| # | 发现 | 改法 | 文件 |
|---|---|---|---|
| 1 | 重要性打分关键词漏判委婉表达 | 已补「牺牲」「没能回来」「离去」等委婉表达关键词 | `long_term_memory.dart` |
| 2 | 传说特质与防崩坏规则冲突 | 移除蛇佬腔/易容等特质与防崩坏规则的直接冲突 | `world_rules.dart` |
| 3 | 人生目标 7 学年才结算 | **三层目标分解**：学年/学期/月度三层子目标，每条含具体描述与叙事引导提示 | `goal_data.dart`（新增 198 行） |
| 5 | 月度事件池深度不足 | **事件池分层**：新增 26 条学年特异性事件（1~7 学年各 4 条），`schoolYearEventText` 按学年 + 种子选事件 | `monthly_event_data.dart`（新增 238 行） |
| 6 | 原创 NPC 生成过慢 | 每学年上限从 4 位提升至 6 位，跨学年自动重置计数 | `mixin_relations.dart` |
| 8 | 传闻系统无传播模型 | `addRumor` 公开化，新增 `_maybeGenerateRumor` 从近期世界事件自动生成传闻（20% 概率），传闻前缀多样化 | `mixin_relations.dart` / `mixin_systems.dart` / `game_provider_base.dart` |

**未收口（标记为后续规划）**：
- #4 世界线变动率 0.3 黑箱 — 涉及 UI 面板，待产品决策
- #7 成长曲线全局总账 — 涉及 UI 面板，待产品决策

---

## 3｜新增机制

### 3.1 属性成长曲线（`balance_constants.dart`）

新增 `growthExpectation` 全局参数表，涵盖 8 项核心属性（dda / potions / charm / transfiguration / herbology / astronomy / history / flying）的年度期望值。

| 属性 | 正常下限 | 正常上限 | 毕业期望 |
|------|---------|---------|---------|
| DDA | 50 | 80 | 65 |
| 魔药学 | 45 | 75 | 60 |
| 变形术 | 42 | 78 | 58 |
| 魔咒学 | 48 | 82 | 62 |
| 草药学 | 40 | 70 | 55 |
| 天文学 | 35 | 65 | 50 |
| 魔法史 | 30 | 60 | 45 |
| 飞行课 | 38 | 72 | 52 |

### 3.2 学院分多元化（`balance_constants.dart` + `mixin_play.dart`）

新增 `houseCupActivityPoints` 统一来源表：

| 活动 | 分值 |
|------|------|
| 魁地奇胜场 | 30 |
| 决斗胜场 | 5 |
| 课堂表现优异 | 3 |
| 禁林探险成功 | 8 |
| 委托完成 | 5 |
| 期末考试前十 | 15 |

`formatHouseCup` 面板同步更新，显示各来源具体分值。

### 3.3 信息密度调节器（`mixin_narrative.dart` + `mixin_narrative_continuity.dart`）

- `calculateInformationDensity`：基于事件信号密度（动作触发 + 动词-对象对 + 时间词 + 环境变化 + 对话）计算叙事密度
- AI 路径：密度 < 0.02 时记录警告，兜底叙事自动增强（追加环境/事件描述）
- 离线路径：密度过低时注入事件种子（5 种预设事件轮换），增强叙事张力
- 字段 `_lastNarrativeDensity` 记录每回合密度，用于调试与调优

### 3.4 学年里程碑（`mixin_systems.dart` + `monthly_event_data.dart`）

- `_onSchoolYearStart` 新增学年里程碑注入，调用 `schoolYearEventText` 按学年生成特有事件叙事
- 涵盖 1~7 学年各 4 条里程碑事件，从分院仪式到 NEWTs 考试
- 事件同时写入 `notifications` 和 `worldState.addNarrativeEvent`

### 3.5 离线模式补齐关键事件（`mixin_narrative.dart`）

- `_finalizeTurn` 后扫描 `worldState.recentEvents`，将当前回合匹配的事件文本追加到 `currentNarrative`
- 确保离线模式下月度事件、学年事件不会丢失

### 3.6 叙事多样性 + 安静期事件（`narrative_prompts.dart` + `mixin_narrative.dart`）

`narrative_prompts.dart` 新增【叙事多样性规则】：
- 5 种开篇方式轮换（动作/对话/感官/内心/事件）
- 节奏变化要求（紧张场景短句密集，平静场景舒展）
- 对话比例合理（不要全叙述或全对话）
- 连续高张力后放缓 / 连续平静时引入小波澜

`mixin_narrative.dart` 新增 `_buildQuietPeriodHint`：
- 连续 3 回合无转折 → 提示注入小波澜（意外来信/奇怪声音/突然出现的同学）
- 连续 5 回合以上无转折 → 强制提示发生实质性事件

### 3.7 传闻传播模型（`mixin_systems.dart`）

- `_maybeGenerateRumor`：从近期世界事件中随机选一条，配以多样化传闻前缀（「最近校园里大家都在议论」「走廊上有人窃窃私语」「有消息灵通的学生透露」等）
- 20% 概率每回合触发，避免刷屏
- `addRumor` 通过 `GameProviderBase` 抽象声明公开化，所有 mixin 均可调用

---

## 4｜经验教训

1. **分段映射优于一刀切压缩**：线性压缩在两端（大事件 vs 日常）同时失效，分段映射保留了"这件事有多重"的语义差异，而结仇通道用 rawDelta 走独立路径的设计值得推广。
2. **社交成本是数值策划的隐形杠杆**：连续互动递减机制（-20%/回合，地板 30%）比单纯的首周上限更贴近真实社交体验——越频繁互动收益越低，促使玩家分散社交。
3. **信息密度是可计算的**：通过事件信号计数/总字数比，可以量化 AI 生成的叙事质量，为自动兜底增强提供可靠触发条件。
4. **安静期检测需要双向阈值**：3 回合触发"小波澜"、5 回合触发"实质性事件"的分级机制，比单一阈值更灵活，避免了"天天提示"和"太晚提示"两种极端。