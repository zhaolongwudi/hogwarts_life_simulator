allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Plugin versions are managed in settings.gradle.
// See https://docs.flutter.dev/to/review-gradle-config for more info.